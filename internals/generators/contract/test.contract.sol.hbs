import "dapple/test.sol";
import "{{ properCase name }}.sol";

contract {{ properCase name }}Test is Test {
  {{ properCase name }} target;

  functon test{{ properCase name }}Method() {
    assertEq(1, 0);
  }
}
